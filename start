#!/usr/bin/env bash
# Lance la visualisation des composants sur la grille.
set -euo pipefail

cd "$(dirname "$0")"

if ! python3 -c "import matplotlib, numpy" 2>/dev/null; then
  echo "Installation des dépendances…"
  python3 -m pip install -r requirements.txt
fi

exec python3 ic_grid.py
